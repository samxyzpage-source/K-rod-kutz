/**
 * Road to Glory: QB — the demo's tiny store (the drive script, the moment index, the box-score line, the seed, the rng)
 *
 * SCAFFOLD STUB — owner: the SHELL agent (replace this file whole).
 *
 * Plain functions, no career engine: the moment screen must depend only on { ctx, Play, PlayView } so the future
 * career can host it unchanged. Settings persist through RTG.UI.Storage; no career save in the demo.
 */
(function (root) {
  'use strict';
  var RTG = root.RTG = root.RTG || {};
  RTG.UI = RTG.UI || {};
})(typeof window !== 'undefined' ? window : globalThis);
